# change
bundle:
  name: sales-cicd-project

include:
  - resources/*.yml

targets:
  dev:
    default: true

    workspace:
      host: https://dbc-7ecc0dbb-562f.cloud.databricks.com

      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}